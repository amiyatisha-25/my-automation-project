{
  "name": "assesstemt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "rm -rf mochawesome-report mochawesome.json",
    "test": "cypress run",
    "posttest": "npm run cy:report",
    "cy:open": "cypress open",
    "cy:run": "cypress run --reporter mochawesome",
    "cy:run:chrome": "cypress run --browser chrome --reporter mochawesome",
    "cy:run:firefox": "cypress run --browser firefox --reporter mochawesome",
   "cy:report": "mochawesome-merge cypress/reports/.jsons/*.json > cypress/reports/merged-report.json && marge cypress/reports/merged-report.json --reportDir cypress/reports --enable-html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^10.0.0",
    "cypress": "^15.1.0",
    "cypress-mochawesome-reporter": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "cypress-downloadfile": "^1.2.4",
    "cypress-file-upload": "^5.0.8",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.9.2"
  }
}